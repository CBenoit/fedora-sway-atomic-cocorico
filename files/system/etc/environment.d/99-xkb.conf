# Set default keyboard layouts and options for the session.  The first
# layout is Bépo (French ergonomic layout) and the second provides
# Japanese input via fcitx's Mozc engine.  ctrl:nocaps makes CapsLock
# behave as an additional Ctrl key.
XKB_DEFAULT_LAYOUT=fr(bepo_afnor)
# XKB_DEFAULT_OPTIONS=ctrl:nocaps
